OpenAI Codex v0.118.0 (research preview)
--------
workdir: /Users/mason/Documents/CODE/axhxrx-megarepo/utf8-stream-occurrences
model: gpt-5.4
provider: openai
approval: never
sandbox: read-only
reasoning effort: xhigh
reasoning summaries: none
session id: 019d9b0c-fec9-72d2-906e-9b7d4c37823d
--------
user
commit HEAD
  → Inspect HEAD diff and changed files
  • Analyze modified code for correctness regressions
  • Produce prioritized review findings in JSON

## Code Review: /Users/mason/Documents/CODE/axhxrx-megarepo/utf8-stream-occurrences @ HEAD

1 finding (1 P2)

The BOM handling change itself looks correct, but this commit also adds an internal `.railgun` transcript that is now included in the package's Deno publish output. That unintended publication side effect makes the patch incorrect.

[P2] Exclude the committed .railgun transcript from publish output
     /Users/mason/Documents/CODE/axhxrx-megarepo/utf8-stream-occurrences/.railgun/2026-04-17-code-review-of-initial-implementation.txt:1-10
     When this package is published to JSR via `deno publish`, `.railgun/` is currently included because `deno.jsonc` does not exclude it, so adding this file makes the full internal review transcript part of the public package. In this repo that leaks the local absolute workspace path, session metadata, and review notes to consumers; if the artifact needs to stay in-repo, it should be excluded from publish output before merging this commit.

---
Author notes (axhxrx-engineer agent, 2026-04-17):

Reviewer is correct. Verified via `deno publish --dry-run --allow-dirty` that `.railgun/` was indeed being bundled into the would-be JSR upload. Added `.railgun`, `.cursor`, and `.git` to `publish.exclude` in `deno.jsonc`. Post-fix dry run shows only README.md, SPEC.md, deno.jsonc, and src/**/*.ts in the simulated upload — nothing under `.railgun/` or `.cursor/`, and no stray `.git` submodule pointer. No code changes.
