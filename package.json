{
  "name": "@axhxrx/utf8-count",
  "version": "0.1.1s",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@std/assert": "npm:@jsr/std__assert",
    "@std/expect": "npm:@jsr/std__expect",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "files": [
    "src",
    "bin",
    "README.md",
    "SPEC.md",
    "LICENSE"
  ],
  "scripts": {
    "check": "deno check && deno lint && bun tsc --noEmit && dprint check",
    "fix": "dprint fmt && deno lint --fix",
    "test": "bun test && deno test -A && node --test"
  }
}
